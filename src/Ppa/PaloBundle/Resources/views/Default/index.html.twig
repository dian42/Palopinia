{% extends '::base.html.twig' %}

{%block body%}
	
    <div id="content" class="span10">
    <h1>Productos</h1>
    
		<div class="row-fluid sortable ui-sortable">
          {% set counter = 1 %} 
				{% for item in entitie %}
					<div class="box span4">
						<div class="box-header well" data-original-title>
							<h2><i class="icon-th"></i> {{item.nombre}}</h2>
						</div>
						<div class="box-content">

						<!-- asdads dasd  -->
								<div id="info" class="tab-pane active"  align="center" >
									<?--<a href="{{ path('pagproducto', {'idProd': item.id})}}">
										{{ render(controller('PpaPaloBundle:Default:picture', {'idProd': item.id } )) }}
									</a>
											<FONT FACE="times new roman" SIZE=3.5 color=0BA457>${{item.precio}}</FONT>
											<p>{{item.descripcionBreve}}</p>
				  						<br><a href="{{ path('pagproducto', {'idProd': item.id})}}" class="btn btn-primary btn-lg active btn btn-info" role="button" shadow: 0 1px 3px rgba(0,0,0,.25)>Características</a>
				  					</p>
								</div>
						<!-- asdads dasd  -->
						</div>
					</div>
					{% if counter  is divisibleby(3) %}
						</div>
						<div class="row-fluid sortable ui-sortable">
					{% endif %}
					{% set counter = counter + 1 %}
				{% endfor %}
			</div>
		</div>
                
{% endblock %}
