{% extends '::base.html.twig' %}

{%block body%}
	
    <div id="content" class="span10">
    <h1>Contactos</h1>
    
		<div class="row-fluid sortable ui-sortable">
          {% set counter = 1 %} 
				{% for item in ent %}
					<div class="box span4">
						<div class="box-header well" align="center" data-original-title>
							<h2>{{item.nombre}}</h2>
						</div>
						<div class="box-content">
							<dl style="line-height: 2px;">
							<dt style="margin-top: -18px;"><center>{{item.cargo}}</center></dt></dl>
							<dd>Teléfono : {{item.telefono}}</dd>
							{% if item.telefono2 != null %}
								<dd>Teléfono (OPC): {{item.telefono2}}</dd>
                            {% endif %}
							<dd>E-mail : {{item.email}}</dd></a>
						</div>
					</div>
					{% if counter  is divisibleby(3) %}
						</div>
						<div class="row-fluid sortable ui-sortable">
					{% endif %}
					{% set counter = counter + 1 %}
				{% endfor %}
			</div>
		</div>
    </div> 
{% endblock %}
