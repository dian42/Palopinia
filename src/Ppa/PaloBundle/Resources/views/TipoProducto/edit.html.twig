{% extends '::baseTablas.html.twig' %}
{% block body -%}
    <h1>TipoProducto edit</h1>

    {{ form(edit_form) }}
        <a class="btn" href="{{ path('tipoproducto') }}">
            Volver a la lista
        </a>{{ form(delete_form) }}
{% endblock %}
