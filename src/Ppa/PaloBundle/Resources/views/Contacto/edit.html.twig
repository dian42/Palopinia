{% extends '::baseTablas.html.twig' %}

{% block body -%}
    <h1>Contacto edit</h1>

    {{ form(edit_form) }}
    <a class="btn" href="{{ path('contacto') }}">
        Volver a la lista
    </a>
    {{ form(delete_form) }}
{% endblock %}
