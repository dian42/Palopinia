{% extends '::baseTablas.html.twig' %}

{% block body -%}
    <h1>Producto edit</h1>

    {{ form(edit_form) }}
    <a class="btn" href="{{ path('producto') }}">
        Volver a la lista
    </a>
    {{ form(delete_form) }}
{% endblock %}
